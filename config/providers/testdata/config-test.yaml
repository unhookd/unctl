---
contexts:
  "org/kube-hello-world":
    - "ci/circleci: build"

deployments:
  test:
    test-deployment:
      release: kube-hello-world
      namespace: kube-hello-world
      repo: org/kube-hello-world
      cluster: local
      branch: master
      chart: tests/chart
