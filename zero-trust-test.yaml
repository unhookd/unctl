---
contexts:
  "org/kube-hello-world":
    - "ci/circleci: build"

endpoints:
  local: http://localhost:8081/zero-trust

deployments:
  test:
    test-deployment:
      release: kube-hello-world
      namespace: kube-hello-world
      repo: org/kube-hello-world
      cluster: local
      branch: master
      chart: tests/chart
