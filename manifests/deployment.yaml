apiVersion: apps/v1
kind: Deployment
metadata:
  name: unhookd
  labels:
    app: unhookd
spec:
  replicas: 1
  template:
    metadata:
      name: unhookd
      labels:
        app: unhookd
    spec:
      containers:
        - name: unhookd
          image: 
          imagePullPolicy: IfNotPresent
      restartPolicy: Always
  selector:
    matchLabels:
      app: unhookd
